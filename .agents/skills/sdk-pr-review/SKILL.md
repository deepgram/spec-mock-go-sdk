---
name: sdk-pr-review
description: Use when reviewing an auto-regen PR (one that touches api/ and has commits from spec-idiomatic-bot). Covers the checklist for verifying the bot did the right thing, signs the bot got something wrong, and when to push back. Route ceremony questions to sdk-breaking-ceremony, agentic-readiness checks to sdk-agentic-readiness.
---

# Reviewing an auto-regen PR

PRs from the fan-out workflow in `deepgram/spec` arrive here with `api/`
regenerated by the codegen plugin and (within seconds) `pkg/` updated by
`spec-idiomatic`. Your job as a reviewer is not to re-do the bot's work
— it's to verify the bot did the right things in the right places.

## The standard PR shape

A clean auto-regen PR looks like this:

1. **Initial commit (from `deepgram/spec`'s fan-out workflow):**
   modifies files under `api/` only. Generated by smithy.
2. **Second commit (from `spec-idiomatic-bot`):**
   modifies files under `pkg/`, plus optionally `Example_*` tests,
   `README.md` opener, `llms.txt`, and `BREAKING_CHANGES.md` at repo
   root.
3. **PR body:** includes the non-conditional work-done checklist
   comment.
4. **CI:** `go build ./...` and `go test ./...` both green.

Anything outside this shape needs explanation in the PR description.

## Review checklist

### Pipeline integrity

- [ ] No file under `api/` is missing its
  `// Code generated by smithy-go-codegen DO NOT EDIT.` header.
- [ ] No human-authored commits to `api/`. If a human touched anything
  in `api/`, it will be wiped by the next regen — push back.
- [ ] The PR head is the bot's facade-update commit, not the raw
  generated-only commit. (If the bot didn't run, find out why before
  merging.)

### Facade absorption (per `sdk-facade-conventions`)

- [ ] No customer-facing public type or func signature in `pkg/` was
  removed.
- [ ] No exported field on a customer-facing struct was removed.
- [ ] Renames of customer-facing fields are accompanied by a tier-2
  entry in `BREAKING_CHANGES.md`.
- [ ] All converters use the canonical deref helpers; no inline nil
  checks on primitive fields.
- [ ] Idiomatic Go casing for initialisms (`RequestID` not
  `RequestId`, `APIVersion` not `ApiVersion`) is applied to every new
  customer field.
- [ ] New union variants added in `api/` are reflected in the
  facade's `ConvertServerMessage` type switch with a corresponding
  customer event type.

### Agentic readiness (per `sdk-agentic-readiness`)

- [ ] Every new public type or function in `pkg/` has an `Example_*`
  in the adjacent `*_test.go`.
- [ ] Every changed signature has its `Example_*` updated.
- [ ] `go test ./...` passes (this exercises every `Example_*`).
- [ ] `README.md` opening paragraph still keyword-dense and unchanged
  in spirit. If the SDK's surface materially changed, the opener was
  updated; verify it still fits the 500-character pattern.
- [ ] `llms.txt` still points at valid files. If a canonical example
  was renamed or moved, `llms.txt` was updated to match.
- [ ] No multi-concept files appeared. The bot refuses to write them
  but humans editing alongside the bot might slip; check for
  `utils.go` or files mixing REST and WebSocket types.

### Breaking ceremony (per `sdk-breaking-ceremony`)

- [ ] If the PR has a `regen/breaking-absorbed` label, there is a
  bot-posted comment titled "⚠️ Absorbed breaking changes" and an
  entry in `BREAKING_CHANGES.md`. Read both. Decide whether the
  absorption is acceptable.
- [ ] If the PR has a `regen/breaking-unavoidable` label, there is a
  bot-posted comment titled "🛑 Unavoidable breaking changes", an
  entry in `BREAKING_CHANGES.md`, and the `spec-idiomatic/breaking-acked`
  status check is failing. You must explicitly apply the
  `regen/breaking-acked` label after deciding the break is acceptable.
- [ ] If there are no breaking labels, there should be no
  `BREAKING_CHANGES.md`. If one is present without a label, that's a
  bot bug — push back.

### Build & test

- [ ] `go build ./...` green.
- [ ] `go test ./...` green (covers `Example_*` smoke tests).
- [ ] No new TODOs or `panic("not implemented")` in `pkg/`.

## Signs the bot got it wrong

- Multiple retry attempts in the workflow log (`attempt 2/3`,
  `attempt 3/3`). The bot is allowed up to 3 retries; if it landed on
  attempt 3 the facade edits may be precarious. Read them carefully.
- A breaking entry in `BREAKING_CHANGES.md` that doesn't match the
  api/ diff (e.g. claims a field was removed but the diff shows it
  was renamed). Push back; ask for a re-run.
- A facade edit that introduces a NEW exported type whose name doesn't
  match the established naming. The bot should be reusing existing
  customer types; new ones need human design intent.
- An `Example_*` that uses `// Output: ...` for non-deterministic
  output (live API calls, randomness). The example will pass once but
  fail in CI later. Push back, ask for a compile-only example.
- The bot deleted an example. Examples are load-bearing for retrieval
  scores; deletions should be tier-2 breaking-acked, not silent.

## When to push back

You should push back (close-without-merging or request changes) when:

1. Customer-facing public surface was broken silently (tier-2 escaped
   ceremony).
2. The facade absorption produced a customer field that's always nil
   when it shouldn't be.
3. The bot reached `max-retries` and the final state has broken
   tests.
4. A multi-concept file appeared.
5. `BREAKING_CHANGES.md` doesn't actually describe the breaking change
   accurately.

In all five cases, the right move is to ping `#team-devrel-internal`
with the PR link and the specific concern. Don't merge a half-broken
regen to "fix in a follow-up" — the next regen will rebuild on top of
it.

## When to ack and merge

Everything in the checklist is green, the work-done comment is
posted, breaking labels (if any) have been acked, and you've read the
facade diff with your own eyes. Merge.

## Related skills

- [`sdk-codegen-flow`](../sdk-codegen-flow/SKILL.md) — what the bot
  did and why.
- [`sdk-facade-conventions`](../sdk-facade-conventions/SKILL.md) —
  the Go-code rules you're verifying.
- [`sdk-agentic-readiness`](../sdk-agentic-readiness/SKILL.md) —
  the example/README/llms.txt rules you're verifying.
- [`sdk-breaking-ceremony`](../sdk-breaking-ceremony/SKILL.md) — how
  to handle each tier of break.
