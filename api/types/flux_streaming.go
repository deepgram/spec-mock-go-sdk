// Code generated by spec-codegen-go. DO NOT EDIT.
//
// WebSocket marshal/unmarshal helpers for every @streaming union
// reachable from the Flux service's operations. Each
// function pair handles the wire-level framing required by the
// @bidiStreamingProtocol contract: @binaryFrame union members ride
// binary WebSocket frames carrying the target structure's
// @eventPayload field bytes; all other members ride JSON text
// frames carrying a "type" discriminator whose value comes from
// the target structure's @messageType trait.
//
// These functions live alongside the shape types they operate on
// so transport-layer code can swap WebSocket implementations
// without touching the wire-level marshalling. The transport
// package keeps a thin set of aliases re-exporting these symbols
// for one regen cycle (see transport/websocket/streaming.go);
// direct callers should migrate to api/types.

package types

import (
	"encoding/json"
	"fmt"
)

// MarshalFluxClientStream encodes a FluxClientStream variant for the wire.
// Returns (payload, isBinary, err). When isBinary is true the payload is raw
// bytes destined for a WebSocket binary frame; otherwise the payload is JSON
// text destined for a WebSocket text frame.
func MarshalFluxClientStream(msg FluxClientStream) ([]byte, bool, error) {
	switch m := msg.(type) {
	case *FluxClientStreamMemberAudio:
		return m.Value.Data, true, nil
	case *FluxClientStreamMemberCloseStream:
		data, err := marshalTagged("CloseStream", m.Value)
		return data, false, err
	case *FluxClientStreamMemberConfigure:
		data, err := marshalTagged("Configure", m.Value)
		return data, false, err
	case nil:
		return nil, false, fmt.Errorf("MarshalFluxClientStream: nil message")
	default:
		return nil, false, fmt.Errorf("MarshalFluxClientStream: unknown variant %T", m)
	}
}

// UnmarshalFluxClientStream decodes a frame into the matching FluxClientStream
// variant. When isBinary is true the bytes are routed directly to the
// @binaryFrame member; otherwise the JSON `type` discriminator selects
// the variant.
func UnmarshalFluxClientStream(data []byte, isBinary bool) (FluxClientStream, error) {
	if isBinary {
		return &FluxClientStreamMemberAudio{Value: FluxAudioFrame{Data: data}}, nil
	}
	var header struct {
		Type string `json:"type"`
	}
	if err := json.Unmarshal(data, &header); err != nil {
		return nil, fmt.Errorf("UnmarshalFluxClientStream: read type: %w", err)
	}
	switch header.Type {
	case "CloseStream":
		var v FluxCloseStream
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxClientStream CloseStream: %w", err)
		}
		return &FluxClientStreamMemberCloseStream{Value: v}, nil
	case "Configure":
		var v FluxConfigure
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxClientStream Configure: %w", err)
		}
		return &FluxClientStreamMemberConfigure{Value: v}, nil
	default:
		return nil, fmt.Errorf("UnmarshalFluxClientStream: unknown type %q", header.Type)
	}
}

// MarshalFluxServerStream encodes a FluxServerStream variant for the wire.
// Returns (payload, isBinary, err). When isBinary is true the payload is raw
// bytes destined for a WebSocket binary frame; otherwise the payload is JSON
// text destined for a WebSocket text frame.
func MarshalFluxServerStream(msg FluxServerStream) ([]byte, bool, error) {
	switch m := msg.(type) {
	case *FluxServerStreamMemberConnected:
		data, err := marshalTagged("Connected", m.Value)
		return data, false, err
	case *FluxServerStreamMemberTurnInfo:
		data, err := marshalTagged("TurnInfo", m.Value)
		return data, false, err
	case *FluxServerStreamMemberConfigureSuccess:
		data, err := marshalTagged("ConfigureSuccess", m.Value)
		return data, false, err
	case *FluxServerStreamMemberConfigureFailure:
		data, err := marshalTagged("ConfigureFailure", m.Value)
		return data, false, err
	case *FluxServerStreamMemberError:
		data, err := marshalTagged("Error", m.Value)
		return data, false, err
	case nil:
		return nil, false, fmt.Errorf("MarshalFluxServerStream: nil message")
	default:
		return nil, false, fmt.Errorf("MarshalFluxServerStream: unknown variant %T", m)
	}
}

// UnmarshalFluxServerStream decodes a frame into the matching FluxServerStream
// variant. The JSON `type` discriminator selects the variant
// (isBinary is unused — this union has no @binaryFrame member).
func UnmarshalFluxServerStream(data []byte, isBinary bool) (FluxServerStream, error) {
	var header struct {
		Type string `json:"type"`
	}
	if err := json.Unmarshal(data, &header); err != nil {
		return nil, fmt.Errorf("UnmarshalFluxServerStream: read type: %w", err)
	}
	switch header.Type {
	case "Connected":
		var v FluxConnected
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxServerStream Connected: %w", err)
		}
		return &FluxServerStreamMemberConnected{Value: v}, nil
	case "TurnInfo":
		var v FluxTurnInfo
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxServerStream TurnInfo: %w", err)
		}
		return &FluxServerStreamMemberTurnInfo{Value: v}, nil
	case "ConfigureSuccess":
		var v FluxConfigureSuccess
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxServerStream ConfigureSuccess: %w", err)
		}
		return &FluxServerStreamMemberConfigureSuccess{Value: v}, nil
	case "ConfigureFailure":
		var v FluxConfigureFailure
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxServerStream ConfigureFailure: %w", err)
		}
		return &FluxServerStreamMemberConfigureFailure{Value: v}, nil
	case "Error":
		var v FluxError
		if err := json.Unmarshal(data, &v); err != nil {
			return nil, fmt.Errorf("UnmarshalFluxServerStream Error: %w", err)
		}
		return &FluxServerStreamMemberError{Value: v}, nil
	default:
		return nil, fmt.Errorf("UnmarshalFluxServerStream: unknown type %q", header.Type)
	}
}
