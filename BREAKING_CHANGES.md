# Breaking changes

This file enumerates the customer-visible changes the facade could not
absorb silently. Each entry is generated by `spec-idiomatic` during a
regen of `pkg/` against a new `api/` snapshot.

## TranscribeInput.MinDuration removed

- **Tier:** 2 (unavoidable-breaking)
- **Shape:** `spectypes.TranscribeInput` (generated, in `api/types`)
- **Member:** `MinDuration`
- **Reason:** Removed from the public wire surface by upstream Smithy spec hygiene (e.g. `@internal` tagging or audit cleanup). The field was not part of the public customer-facing API.
- **What this PR did to the SDK:**
  - The facade did not expose `MinDuration`; no facade struct change applied.
  - No matching `TestWires_/TestDropped_` test function existed.
- **Authored by:** the spec-idiomatic post-LLM tier-2 cleanup pass. This entry describes the actual code change applied in this run; it is not a forecast of how a production SDK ought to absorb a tier-2 removal. A future regen on a production SDK that elects a source-compat shim should write its own entry describing whatever shim it left behind.

## TranscribeInput.MaxDuration removed

- **Tier:** 2 (unavoidable-breaking)
- **Shape:** `spectypes.TranscribeInput` (generated, in `api/types`)
- **Member:** `MaxDuration`
- **Reason:** Removed from the public wire surface by upstream Smithy spec hygiene (e.g. `@internal` tagging or audit cleanup). The field was not part of the public customer-facing API.
- **What this PR did to the SDK:**
  - The facade did not expose `MaxDuration`; no facade struct change applied.
  - No matching `TestWires_/TestDropped_` test function existed.
- **Authored by:** the spec-idiomatic post-LLM tier-2 cleanup pass. This entry describes the actual code change applied in this run; it is not a forecast of how a production SDK ought to absorb a tier-2 removal. A future regen on a production SDK that elects a source-compat shim should write its own entry describing whatever shim it left behind.

## TranscribeInput.PhonemeLattice removed

- **Tier:** 2 (unavoidable-breaking)
- **Shape:** `spectypes.TranscribeInput` (generated, in `api/types`)
- **Member:** `PhonemeLattice`
- **Reason:** Removed from the public wire surface by upstream Smithy spec hygiene (e.g. `@internal` tagging or audit cleanup). The field was not part of the public customer-facing API.
- **What this PR did to the SDK:**
  - Removed the facade member referencing `PhonemeLattice` from its `*Options` struct.
  - No matching `TestWires_/TestDropped_` test function existed.
- **Authored by:** the spec-idiomatic post-LLM tier-2 cleanup pass. This entry describes the actual code change applied in this run; it is not a forecast of how a production SDK ought to absorb a tier-2 removal. A future regen on a production SDK that elects a source-compat shim should write its own entry describing whatever shim it left behind.

## TranscribeInput.SpeakersOfInterest removed

- **Tier:** 2 (unavoidable-breaking)
- **Shape:** `spectypes.TranscribeInput` (generated, in `api/types`)
- **Member:** `SpeakersOfInterest`
- **Reason:** Removed from the public wire surface by upstream Smithy spec hygiene (e.g. `@internal` tagging or audit cleanup). The field was not part of the public customer-facing API.
- **What this PR did to the SDK:**
  - Removed the facade member referencing `SpeakersOfInterest` from its `*Options` struct.
  - No matching `TestWires_/TestDropped_` test function existed.
- **Authored by:** the spec-idiomatic post-LLM tier-2 cleanup pass. This entry describes the actual code change applied in this run; it is not a forecast of how a production SDK ought to absorb a tier-2 removal. A future regen on a production SDK that elects a source-compat shim should write its own entry describing whatever shim it left behind.

## TranscribeInput.DetectLanguageVersion removed

- **Tier:** 2 (unavoidable-breaking)
- **Shape:** `spectypes.TranscribeInput` (generated, in `api/types`)
- **Member:** `DetectLanguageVersion`
- **Reason:** Removed from the public wire surface by upstream Smithy spec hygiene (e.g. `@internal` tagging or audit cleanup). The field was not part of the public customer-facing API.
- **What this PR did to the SDK:**
  - The facade did not expose `DetectLanguageVersion`; no facade struct change applied.
  - No matching `TestWires_/TestDropped_` test function existed.
- **Authored by:** the spec-idiomatic post-LLM tier-2 cleanup pass. This entry describes the actual code change applied in this run; it is not a forecast of how a production SDK ought to absorb a tier-2 removal. A future regen on a production SDK that elects a source-compat shim should write its own entry describing whatever shim it left behind.

